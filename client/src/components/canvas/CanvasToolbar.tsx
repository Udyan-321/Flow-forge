export default function CanvasToolbar({ isCreate, workflowname, message, onNameChange, onAddNode, onSave, onViewRuns }: { isCreate: boolean; workflowname: string; message: string; onNameChange: (value: string) => void; onAddNode: (value: string) => void; onSave: () => void; onViewRuns: () => void }) {
  return <div className="canvas-toolbar">{!isCreate && <button className="btn btn--ghost" onClick={onViewRuns}>View past runs</button>}<input type="text" className="input canvas-toolbar__title-input" placeholder="Enter workflow name" value={workflowname} onChange={(event) => onNameChange(event.target.value)} /><select className="select" value="" onChange={(event) => onAddNode(event.target.value)}><option disabled value="">+ Add node</option><option value="githubTrigger">githubTrigger</option><option value="aiReview">aiReview</option><option value="diffQualityConditional">diffQualityConditional</option><option value="postComment">postComment</option><option value="slackNotif">slackNotif</option></select>{message && <span className="canvas-toolbar__status">{message}</span>}<span className="canvas-toolbar__spacer" /><button className="btn btn--primary" onClick={onSave}>Save</button></div>;
}
