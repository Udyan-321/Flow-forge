import { useNavigate, useParams } from "react-router-dom";
import { useRuns } from "../hooks/useRuns";
import "../styles/components.css";
export default function RunListPage() { const { workflowId } = useParams(); const navigate = useNavigate(); const runs = useRuns(workflowId); if (!workflowId || runs.length === 0) return <div className="page"><div className="page__body"><div className="empty-state">No past runs</div></div></div>; return <div className="page"><div className="page__body"><div className="page-header"><h2 className="page-title">Past runs</h2></div><div className="list">{runs.map((run) => <div key={run.id} className="list-row" onClick={() => navigate(`/runs/${run.id}`)}><div className="list-row__main"><div className="list-row__title"><span className={`badge badge--${run.status === "completed" ? "ok" : run.status === "failed" ? "err" : run.status === "started" ? "warn" : "idle"}`}>{run.status}</span></div><div className="list-row__meta"><span>started {run.startedAt}</span></div></div><span className="list-row__chevron">›</span></div>)}</div></div></div>; }
